This directory is currently unused.

The site runs on Astro `output: "server"` with the Cloudflare Pages adapter, so API routes live under:

- `src/pages/api/*`

If you later add Cloudflare Pages Functions outside of Astro, document them here.
