# Generated content directory.
#
# Files in this directory are written by `bun run sync:projects` (which
# discovers all GitHub repos tagged with the `codeseys-embed` topic, fetches
# their `web.codeseys.json` manifests, and validates them against the zod
# schema in `src/lib/types/project-manifest.ts`).
#
# DO NOT hand-edit anything in here. Edit the manifest at the source repo
# instead, then re-run sync.
#
# See ../../docs/PROJECT_EMBEDS.md for the architecture.
